# Example environment variables for History Practice App backend

# MongoDB Atlas connection string (replace with your own URI)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority&appName=<appName>

# Port to run the backend server (Render ignores this, but useful locally)
PORT=5000
